# Cloudflare Worker configuration for narrative generation proxy
# See: https://developers.cloudflare.com/workers/wrangler/configuration/

name = "therapy-docs-narrative"
main = "worker.js"
compatibility_date = "2024-01-01"

# Uncomment to use custom domain (set up in Cloudflare dashboard first)
# routes = [
#   { pattern = "narrative.jwv.dev", custom_domain = true }
# ]

# Environment variables (secrets should be added via wrangler secret put)
# DO NOT put ANTHROPIC_API_KEY here - use: wrangler secret put ANTHROPIC_API_KEY

[observability]
enabled = true
